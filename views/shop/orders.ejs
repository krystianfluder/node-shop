<%- include('../includes/head.ejs') %>
<link rel="stylesheet" href="/css/cart.css">
</head>

<body>
    <%- include('../includes/navigation.ejs') %>
    <main>
        <% if (orders.length > 0) { %>
        <ul class="cart__item-list">
            <% orders.forEach(order => { %>
            <li class="cart__item">
                <h1>Order</h1>
                <h2>Quantity: <%= order.createdAt %></h2>

            </li>
            <% })%>
        </ul>
        <% } else { %>
        <h1>No Orders!</h1>
        <% } %>
    </main>
    <%- include('../includes/end.ejs') %>